spring:
  application:
    name: api-service

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    service-url:
      defaultZone: http://test:123456@127.0.0.1:8761/eureka
